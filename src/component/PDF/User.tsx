interface User {
    name: string;
    address: string;
    owner: string;
    rent: number; // Assuming rent is a number, adjust if needed
    pan: string;
    startDate: string;
    endDate: string;
  }
  

export default User;